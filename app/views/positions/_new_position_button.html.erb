<% available_positions = 3 - @positions_count %>
<% if available_positions > 0 %>
  <%= link_to "Neue Position erstellen (#{available_positions} positionen verfügbar)", new_position_path, class: "inline-block mb-8 px-4 py-2 rounded-md bg-green-500 text-black border border-slate-200 hover:bg-green-700 my-8", role: 'button' %>
<% else %>
  <span class="inline-block mb-8 px-4 py-2 rounded-md bg-gray-500 text-gray-300 border border-gray-400 cursor-not-allowed">Neue Position erstellen (0 positionen verfügbar)</span>
<% end %>