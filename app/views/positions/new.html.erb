<!-- filepath: /Users/davidrischow/Desktop/visualStudio/LetsGoVolunteer-Backend/app/views/positions/new.html.erb -->
<h1 class="text-3xl font-bold mb-6">Neue Position erstellen</h1>

<%= form_for @position, url: positions_path, html: { class: "space-y-4", multipart: true } do |f| %>
  <div class="field">
    <%= f.label :title, "Bezeichnung der Position", class: "block text-sm font-medium text-gray-700" %>
    <%= f.text_field :title, class: "w-full mt-1 px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
  </div>
  <div id="word-count-title" class="mt-1 rounded-md focus:outline-none"> </div>

  <div class="field">
    <%= f.label :description, "Beschreibung der Position", class: "block text-sm font-medium text-gray-700" %>
    <%= f.text_area :description, class: "w-full mt-1 px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
  </div>
  <div id="word-count-description" class="mt-1 rounded-md focus:outline-none"> </div>

  <div class="field">
    <%= f.label :benefits, "Benefits für ehrenamtliche", class: "block text-sm font-medium text-gray-700" %>
    <%= f.text_area :benefits, class: "w-full mt-1 px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
  </div>
  <div id="word-count-benefits" class="mt-1 rounded-md focus:outline-none"> </div>

  <div class="field">
    <%= f.label :picture, "Titelbild", class: "block text-sm font-medium text-gray-700" %>
    <div class="border-2 border-gray-300 p-4 rounded-md">
      <%= f.file_field :picture, accept: 'image/*' %>
      <p class="text-sm text-gray-500">Bitte ein Titelbild hochladen</p>
    </div>
  </div>

  <div class="field">
    <%= f.label :additional_pictures, "Weitere Bilder", class: "block text-sm font-medium text-gray-700" %>
    <div class="space-y-4">
      <div class="border-2 border-gray-300 p-4 rounded-md">
        <%= f.file_field :additional_pictures, accept: 'image/*', multiple: true %>
        <p class="text-sm text-gray-500">Bitte ein Bild hochladen</p>
      </div>
      <div class="border-2 border-gray-300 p-4 rounded-md">
        <%= f.file_field :additional_pictures, accept: 'image/*', multiple: true %>
        <p class="text-sm text-gray-500">Bitte ein Bild hochladen</p>
      </div>
      <div class="border-2 border-gray-300 p-4 rounded-md">
        <%= f.file_field :additional_pictures, accept: 'image/*', multiple: true %>
        <p class="text-sm text-gray-500">Bitte ein Bild hochladen</p>
      </div>
    </div>
  </div>
    <div class="field">
    <label class="block text-sm font-medium text-gray-700">Kreativität</label>
    <div>
      <% (1..5).each do |i| %>
        <label>
          <input type="radio" name="input1" value="<%= i %>" class="rating-input"> <%= i %>
        </label>
      <% end %>
    </div>
  </div>

  <div class="field">
    <label class="block text-sm font-medium text-gray-700">Technische Skills</label>
    <div>
      <% (1..5).each do |i| %>
        <label>
          <input type="radio" name="input2" value="<%= i %>" class="rating-input"> <%= i %>
        </label>
      <% end %>
    </div>
  </div>

  <div class="field">
    <label class="block text-sm font-medium text-gray-700">Soziale Skills</label>
    <div>
      <% (1..5).each do |i| %>
        <label>
          <input type="radio" name="input3" value="<%= i %>" class="rating-input"> <%= i %>
        </label>
      <% end %>
    </div>
  </div>

    <div class="field">
    <label class="block text-sm font-medium text-gray-700">Sprachliche Skills</label>
    <div>
      <% (1..5).each do |i| %>
        <label>
          <input type="radio" name="input4" value="<%= i %>" class="rating-input"> <%= i %>
        </label>
      <% end %>
    </div>
  </div>

      <div class="field">
    <label class="block text-sm font-medium text-gray-700">Flexibilität</label>
    <div>
      <% (1..5).each do |i| %>
        <label>
          <input type="radio" name="input5" value="<%= i %>" class="rating-input"> <%= i %>
        </label>
      <% end %>
    </div>
  </div>

  <div id="remaining-points" class="mt-4 text-sm font-medium text-gray-700">Verbleibende Punkte: 10</div>

  <div class="actions mt-10">
    <%= f.submit "Erstellen", class: "w-full py-2 px-4 bg-blue-600 text-white font-medium text-sm rounded-md shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2" %>
  </div>
<% end %>

<script>
  document.addEventListener("DOMContentLoaded", function() {
    function updateWordCount(field, counter, minLength, goodLength, perfectLength) {
      const charCount = field.value.length;

      if (charCount < minLength) {
        counter.textContent = `Du benötigst noch mindestens ${minLength - charCount} Buchstaben`;
        counter.style.color = 'red';
      } else if (charCount < goodLength) {
        counter.textContent = `Gute Länge (${minLength} - ${goodLength} Buchstaben)`;
        counter.style.color = 'yellow';
      } else if (charCount < perfectLength) {
        counter.textContent = `Perfekte Länge (${goodLength} - ${perfectLength} Buchstaben)`;
        counter.style.color = 'green';
      } else {
        counter.textContent = 'Zu viele Buchstaben';
        counter.style.color = 'red';
      }
    }

    const titleField = document.querySelector('input[name="position[title]"]');
    const descriptionField = document.querySelector('textarea[name="position[description]"]');
    const benefitsField = document.querySelector('textarea[name="position[benefits]"]');
    const maxPoints = 16;
    const ratingInputs = document.querySelectorAll('.rating-input');
    const remainingPointsDisplay = document.getElementById('remaining-points');

    const wordCountTitle = document.getElementById('word-count-title');
    const wordCountDescription = document.getElementById('word-count-description');
    const wordCountBenefits = document.getElementById('word-count-benefits');

    titleField.addEventListener('input', function() {
      updateWordCount(titleField, wordCountTitle, 10, 16, 26);
    });

    descriptionField.addEventListener('input', function() {
      updateWordCount(descriptionField, wordCountDescription, 300, 700, 1000);
    });

    benefitsField.addEventListener('input', function() {
      updateWordCount(benefitsField, wordCountBenefits, 300, 700, 1000);
    });

    function updateRemainingPoints() {
      let totalPoints = 0;
      ratingInputs.forEach(input => {
        if (input.checked) {
          totalPoints += parseInt(input.value);
        }
      });

      const remainingPoints = maxPoints - totalPoints;
      remainingPointsDisplay.textContent = `Verbleibende Punkte: ${remainingPoints}`;

      if (remainingPoints < 0) {
        remainingPointsDisplay.style.color = 'red';
      } else {
        remainingPointsDisplay.style.color = 'black';
      }
    }

    ratingInputs.forEach(input => {
      input.addEventListener('change', updateRemainingPoints);
    });

    updateRemainingPoints();
  });
</script>